sudo pacman -S python-pip - AUR Linux (Arch Linux, Manjaro Linux, ArcoLinux ....)
sudo apt -y install python3-pip - advanced packaging tool (Ubuntu, Debian, Mint....)

pip install numpy
pip install pandas
pip install notebook
pip install matplotlib
pip install seaborn
pip install plotly
pip install statistics
pip install scikit-learn
pip install category_encoders
pip install scipy
pip install statsmodels
pip install comet_ml
pip install lightautoml
pip install beautifulsoup4
pip install requests
pip install -U ydata-profiling
pip install ipywidgets
pip install sweetviz
pip install dtale
pip install category_encoders
pip install imbalanced-learn
pip install -U lightautoml


БОНУС. EDA одной строкой кода
pip install pandas-profiling
pip install sweetviz
pip install dtale

pip install pandas-profiling && pip install sweetviz && pip install dtale

pip install numpy && pip install pandas && pip install notebook && pip install matplotlib && pip install seaborn && pip install plotly && pip install statistics && pip install scikit-learn && pip install category_encoders && pip install scipy && pip install statsmodels && pip install comet_ml && pip install lightautoml && pip install beautifulsoup4 && pip install requests 

pip install -U ydata-profiling && pip install ipywidgets && pip install sweetviz && pip install dtale && pip install category_encoders && pip install imbalanced-learn && pip install -U lightautoml


 

=)
